module.exports = {
  // webpack4后，环境 process.env.NODE_ENV 默认为 "production"
  // env: {
    // NODE_ENV: '"production"'
  // },
  // define global constants for application see https://webpack.js.org/plugins/define-plugin/
  defineConstants: {
  }
}
