#!/usr/bin/env node

const program = require('commander')

const {
  getAthenaVersion
} = require('../src/util')

program
  .version(getAthenaVersion())
  .usage('<command> [options]')
  .command('init [url]', 'Init Athena')
  .command('app [appName]', 'Create an app').alias('a')
  .command('serve [name]', 'Development preview').alias('s')
  .parse(process.argv)
